class TransactionItem {
  String? createdAt;
  String? id;
  String? to;
  String? time;
  String? amount;
  String? type;

  TransactionItem(
      {this.createdAt, this.id, this.to, this.time, this.amount, this.type});
}
