class DebitCardLimit {
  DebitCardLimit({
    this.description,
    this.minLimit,
    this.currentLimit,
    this.maxLimit,
  });

  String? description;
  String? minLimit;
  String? currentLimit;
  String? maxLimit;
}
